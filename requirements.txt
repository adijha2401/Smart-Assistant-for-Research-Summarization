# Core backend
fastapi
uvicorn
pydantic
python-multipart
requests
python-dotenv

# PDF file reading
pdfplumber

# Frontend
streamlit

# Optional improvements
tqdm         # Progress bars if needed
httpx        # Alternative to requests (useful in async contexts)

# Compatibility (optional but recommended)
certifi      # Ensures SSL certificates are valid in some environments
